// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
	provider = "mysql"
	url = env("DATABASE_URL")
}
generator client {
	provider = "prisma-client-js"
}
model Address {
	id Int @id @default(autoincrement())
	zipecode String 
	street String 
	number Int 
	complement String? 
	city String 
	district String 
	state String 
	createdAt DateTime @default(now()) @map("created_at")
	updatedAt DateTime @updatedAt @map("updated_at")
	clients Client[] @relation(name: "AddressToClient")
	@@map("addresses")
}
model Client {
	id Int @id @default(autoincrement())
	addressId Int @map("address_id")
	address Address @relation(name: "AddressToClient", fields: [addressId], references: [id])
	cpf String 
	cnpj String @unique
	name String 
	cell String 
	phone String 
	email String 
	clientType ClientType 
	createdAt DateTime @default(now()) @map("created_at")
	updatedAt DateTime @updatedAt @map("updated_at")
	@@map("clients")
}
enum ClientType {
	PHYSICAL_PERSON
	LEGAL_PERSON
}